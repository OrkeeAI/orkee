# ============================================================================
# Orkee Environment Configuration
# ============================================================================
# 
# Most settings are managed via the Settings UI in the dashboard!
# Only essential bootstrap settings that must be configured before the 
# application starts are required in this .env file.
#
# To configure other settings (security, rate limiting, TLS, etc.):
# 1. Start Orkee: `orkee dashboard`
# 2. Navigate to Settings tab
# 3. Configure via the UI - changes persist in the database
# ============================================================================

# ----------------------------------------------------------------------------
# Bootstrap Configuration (Required)
# ----------------------------------------------------------------------------
# These settings control how the application starts and cannot be changed
# at runtime. They are read-only in the Settings UI.

ORKEE_API_PORT=4001                                   # API server port
ORKEE_UI_PORT=5173                                    # Dashboard UI port
ORKEE_DEV_MODE=false                                  # Development mode: use local dashboard instead of ~/.orkee/dashboard/

# ----------------------------------------------------------------------------
# Cloud Authentication (Optional)
# ----------------------------------------------------------------------------
# Sign up at https://orkee.ai to get your cloud token.
# Generate a token at: https://app.orkee.ai/settings/tokens
# 
# Cloud sync can be enabled/disabled via Settings UI, but the token must be 
# configured here for security.

# ORKEE_CLOUD_TOKEN=ok_live_abc123...                 # Your Orkee Cloud authentication token

# ----------------------------------------------------------------------------
# Database-Managed Settings
# ----------------------------------------------------------------------------
# All other settings are configured via the Settings UI:
# 
# Settings > General:    Editor integration, preferences
# Settings > Security:   CORS, directory browsing, security headers
# Settings > Database:   Import/export, data management
# Settings > Privacy:    Telemetry, error reporting, usage metrics
# Settings > Cloud:      Cloud sync, API URL
# Settings > Advanced:   Rate limiting, TLS/HTTPS, certificates
#
# These settings persist in the database and don't need to be in .env

# ----------------------------------------------------------------------------
# Advanced Configuration
# ----------------------------------------------------------------------------
# Usually auto-configured, but can be overridden if needed

# VITE_API_URL="http://localhost:4001"                # Override API URL (auto-constructed from ORKEE_API_PORT)
