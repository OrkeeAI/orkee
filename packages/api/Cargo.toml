[package]
name = "orkee-api"
version.workspace = true
edition.workspace = true
description = "HTTP API layer and routing for Orkee"
authors.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "orkee_api"

[dependencies]
# Projects package (for DbState and manager functions)
orkee-projects = { path = "../projects" }

# Core packages
orkee-core = { path = "../core" }
orkee-config = { path = "../config" }
orkee-storage = { path = "../storage" }
orkee-security = { path = "../security" }
orkee-context = { path = "../context" }
orkee-tasks = { path = "../tasks" }
orkee-tags = { path = "../tags" }
orkee-settings = { path = "../settings" }
orkee-agents = { path = "../agents" }
orkee-executions = { path = "../executions" }
orkee-ai = { path = "../ai" }
orkee-models = { path = "../models" }
orkee-ideate = { path = "../ideate" }
orkee-auth = { path = "../auth" }

# Database
sqlx = { version = "0.8", features = ["runtime-tokio", "sqlite", "macros"] }

# Web framework
axum = "0.8"
tower = "0.5"
tower-http = "0.6"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Async runtime
tokio = { version = "1.0", features = ["full"] }
tokio-stream = "0.1"
futures = "0.3"
async-stream = "0.3"

# Tracing
tracing = "0.1"

# DateTime handling
chrono = { version = "0.4", features = ["serde"] }

# HTTP client
reqwest = { version = "0.12", features = ["json", "stream", "gzip", "brotli", "deflate"] }
url = "2.5"

# ID generation
nanoid = "0.4"
uuid = { version = "1.0", features = ["v4", "serde"] }

# Security
zeroize = "1.8"

[dev-dependencies]
orkee-projects = { path = "../projects", features = ["test-utils"] }
tokio-test = "0.4"
http-body-util = "0.1"
