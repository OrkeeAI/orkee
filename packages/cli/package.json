{
  "name": "@orkee/cli",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "cargo build --release --features cloud",
    "dev": "cargo run --bin orkee -- dashboard",
    "test": "cargo test --features cloud"
  }
}