{
  "name": "@orkee/projects",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "dev": "echo 'Projects library - no dev server needed. Run tests with: pnpm test'",
    "test": "cargo test"
  }
}