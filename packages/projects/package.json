{
  "name": "@orkee/projects",
  "version": "0.0.9",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "dev": "echo 'Projects library - no dev server needed. Run tests with: pnpm test'",
    "test": "echo 'Skipping orkee-projects tests (requires DATABASE_URL)' && exit 0"
  }
}
